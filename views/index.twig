{% extends 'layout.twig' %}

{% block body %}
    <style>
        body {
            background: rgba(13, 166, 217, 0.1);

        }
        .zoom-effect {
            transition: transform 0.3s;


        }

        .zoom-effect:hover {
            transform: scale(1.1);
            /*box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);*/

        }
    </style>
    <div class="container ">
        <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
            <div class="col-md-3 mb-2 mb-md-0">
                <a href="/" class="d-inline-flex link-body-emphasis text-decoration-none">
                    Сформувати запит для вибору ЕНР:
                </a>
            </div>

            <div class="col-md-3 text-end">
                {% if user %}
                    {% if user.role == "admin" %}
                        <a href="{{ path:('admin/panel') }}"><button type="button" class="btn btn-outline-primary me-2 ">Панель Адміністратора</button></a>
                        <a href="{{ path:('logout') }}"><button type="button" class="btn btn-outline-primary me-2 ">Вийти</button></a>
                    {% else %}
                        <a class="btn btn-primary" href="/logout">Вийти</a>
                    {% endif %}

                {% else %}
                    <a href="{{ path:('login') }}"><button type="button" class="btn btn-outline-primary me-2  zoom-effect">Увійти</button></a>
                    <a href="{{ path:('register') }}"><button type="button" class="btn btn-primary zoom-effect">Зареєструватися</button></a>
                {% endif %}
            </div>
        </header>
    </div>

    <form action="/apply-resource" method="post" class="container">
        <div>
            <fieldset>
                <legend>Оберіть тип навчальної системи:</legend>
                <div class="d-flex align-items-center">
                    <select class="form-select w-200 zoom-effect" name="type_of_resource" id="type_of_resource">
                        {% for category in categories %}
                            {% set name = category.name %}
                            <option value="{{ name }}">{{ name }}</option>
                        {% endfor %}
                    </select>
                </div>
            </fieldset>
        </div>

        <div>
            <fieldset>
                <legend style="margin-top: 30px">Оберіть бажані критерії</legend>

                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Інтерактивність 0" id="k1">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k1">Інтерактивність</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Мультимедійність 1" id="k2">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k2">Мультимедійність</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Можливість модифікації 2" id="k3">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k3">Можливість модифікації</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Кросплатформеність 3" id="k4">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k4">Кросплатформеність</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Вільнопоширюваність 4" id="k5">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k5">Вільнопоширюваність</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Архітектура 5" id="k6">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k6">Архітектура</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Функціональність 6" id="k7">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k7">Функціональність</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Чисельність тем для опрацювання 7" id="k8">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k8">Чисельність тем для опрацювання</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Відповідність предметній області 8" id="k9">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k9">Відповідність предметній області</label>
                    </div>
                </div>
                <div class="">
                    <div class="form-check d-flex align-items-center my-3">
                        <input class="form-check-input" type="checkbox" name="check" value="Відповідність навчального змісту освітнім стандартам 9" id="k10">
                        <select class="form-select mx-3 zoom-effect" name="importance" style="width: 100px;">
                            {% for value in 9..1 %}
                                <option value="{{ value }}">{{ value }}</option>
                            {% endfor %}
                        </select>
                        <label class="form-check-label zoom-effect" for="k10">Відповідність навчального змісту освітнім стандартам</label>
                    </div>
                </div>
            </fieldset>
        </div>
        <div class="text-center my-3">
            <button class="btn btn-primary zoom-effect" type="submit">Вибрати</button>
        </div>
    </form>
{% endblock %}
s